apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-application2
  labels:
    app: demo-application2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-application2
  template:
    metadata:
      labels:
        app: demo-application2
    spec:
      containers:
        - name: demo-application2
          image: gcr.io/my-kubernetes-project-389604/gke-deployment-ci-cd
          ports:
            - containerPort: 8080